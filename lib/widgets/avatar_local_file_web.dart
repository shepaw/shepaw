import 'package:flutter/material.dart';

/// Web stub: local filesystem avatars are unavailable in the browser.
Widget svgFile(
  String path, {
  required double width,
  required double height,
  required BoxFit fit,
  required Widget placeholder,
}) {
  return placeholder;
}

/// Web stub: local filesystem avatars are unavailable in the browser.
Widget rasterFile(
  String path, {
  required double width,
  required double height,
  required BoxFit fit,
  required Widget fallback,
}) {
  return fallback;
}
